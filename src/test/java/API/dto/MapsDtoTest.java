/*package API.dto;

import com.epf.dto.MapsDto;
import org.junit.Test;
import static org.junit.Assert.*;

public class MapsDtoTest {

    @Test
    public void testMapsDtoConstructorAndGetters() {
        MapsDto dto = new MapsDto(1L, 3, 4, "path/to/image.png");
        assertEquals(Long.valueOf(1), dto.getIdMap());
        assertEquals(3, dto.getLigne());
        assertEquals(4, dto.getColonne());
        assertEquals("path/to/image.png", dto.getCheminImage());
    }

    @Test
    public void testMapsDtoSetters() {
        MapsDto dto = new MapsDto();
        dto.setIdMap(2L);
        dto.setLigne(5);
        dto.setColonne(6);
        dto.setCheminImage("new/path.png");

        assertEquals(Long.valueOf(2), dto.getIdMap());
        assertEquals(5, dto.getLigne());
        assertEquals(6, dto.getColonne());
        assertEquals("new/path.png", dto.getCheminImage());
    }
} */
